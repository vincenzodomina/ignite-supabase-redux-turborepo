{
  "name": "@repo/store",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.2.7",
    "react-redux": "^9.1.2"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "tsup": "^8.2.4"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  }
}
